<!DOCTYPE html>
<html lang="en">

  <head>
    <title>Homepage</title>
    <link rel="stylesheet" href="styles.css">
  </head>

  <body>

    <h1>Welcome to the League of Cool Kickball Professionals Webpage!</h1>


    <ul>
      <% @@contacts.each do |team_member| %>

        <% if current_member != team_member[:team] %>

        <li>
          <a href="<%= url("/team/#{team_member[:team]}") %>">
            <%= "#{team_member[:team]}" %>
          </a>
        </li>

        <% else %>
        <%= current_member = team_member[:team]%>
        <% end %>

      <% end %>
    </ul>

  </body>

</html>
